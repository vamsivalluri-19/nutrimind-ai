# AI Models

This folder mirrors the model layer described in the product brief.

## Included placeholders
- `food-detection/` - food recognition and nutrient estimation
- `recommendation-engine/` - personalized diet generation
- `prediction-models/` - weight, BMI, and calorie forecasting

The current implementation uses deterministic mock logic so the full product surface is runnable before real training pipelines are connected.
